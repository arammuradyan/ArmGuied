<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">
<scale android:pivotX="50%" android:pivotY="50%" android:fromYScale="0.8" android:toYScale="1.0"
    android:fromXScale="0.8" android:toXScale="1.0" android:duration = "1500"  android:repeatMode="restart"
    android:repeatCount="infinite" android:interpolator="@android:anim/cycle_interpolator">
</scale>
    <alpha android:fromAlpha="0.3" android:toAlpha="0.7" android:repeatMode="restart"
        android:duration = "1500"
        android:repeatCount="infinite" android:interpolator="@android:anim/cycle_interpolator">
    </alpha>
</set>